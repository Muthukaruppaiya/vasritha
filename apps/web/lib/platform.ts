/** Internal ops platform (admin / POS / staff tools). */
export const OPS_PLATFORM_NAME = "Sukadhaa";

/** Default customer-facing sales brand code. */
export const DEFAULT_BRAND_CODE = "VASRITHA";

/** Default customer-facing sales brand display name. */
export const DEFAULT_BRAND_NAME = "Vasritha";

/**
 * Canonical public domains (documentation defaults).
 * Runtime host lists come from STOREFRONT_HOSTS / OPS_HOSTS env vars.
 */
export const DEFAULT_STOREFRONT_DOMAIN = "vasritha.in";
export const DEFAULT_OPS_DOMAIN = "sukadhaa.in";
